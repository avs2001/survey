import { Routes } from '@angular/router';
import { NotificationSettingsPage } from './settings/notification-settings.page';

export const NOTIFICATIONS_ROUTES: Routes = [
  { path: '', component: NotificationSettingsPage },
];
